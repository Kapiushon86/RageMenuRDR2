#ifndef MOUNT_DEFAULT_STATES_H
#define MOUNT_DEFAULT_STATES_H

#include <vector>
#include <string>
#include <thread>
#include <atomic>

extern bool mount_apply_to_owned_mount;
extern bool mount_invincibility_bool;
extern bool mount_invisibility_bool;
extern bool mount_infinite_stamina_bool;
extern bool mount_was_infinite_stamina_bool;
extern bool mount_no_ragdoll_bool;
extern bool mount_fearless_bool;
extern bool mount_fire_hooves_bool;
extern bool mount_teleport_mount_whistle_bool;
extern bool mount_always_show_cores_bool;
extern bool mount_always_clean_bool;

// Vectors 

extern int preferenceOptionIndex;
extern std::vector<std::string> preferenceOptionVector;

#endif
